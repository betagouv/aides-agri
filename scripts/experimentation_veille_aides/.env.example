# Example environment configuration for aides-agri
# Copy to `.env` at the project root and replace placeholder values.

# --- Observaiblity ---
# Langfuse tracing keys (used for monitoring AI workflows) -> 1 key per different project
LANGFUSE_PUBLIC_KEY=your_langfuse_public_key_here
LANGFUSE_SECRET_KEY=your_langfuse_secret_key_here
# If self-hosting Langfuse, override host (default: cloud.langfuse.com)
LANGFUSE_HOST=https://cloud.langfuse.com
